"""
Given an array nums of integers, return how many of them contain an even number of digits.
"""

import math

class Solution:
    def findNumbers(self, nums):
        count = 0
        for num in nums:
            digits = int(math.log10(num)) + 1  # Count digits
            if digits % 2 == 0:
                count += 1
        return count
